/**
 * Special handler for direct navigation to event URLs
 */
document.addEventListener('DOMContentLoaded', function() {
    // This file is only for handling direct URL navigation to event detail pages
    // The router.js file handles the normal SPA navigation
    
    // No need to do anything here - router.js will handle everything
    console.log("event-details.js loaded - URL handling delegated to router.js");
});